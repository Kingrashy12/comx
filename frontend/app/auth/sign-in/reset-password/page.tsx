import { ResetPassword } from "@/components";
import React from "react";

export const metadata = {
  title: "Reset Password - ComX",
};

const Page = () => {
  return <ResetPassword />;
};

export default Page;
