import { Success } from "@/components";
import React from "react";

export const metadata = {
  title: "Password Update Successful - ComX",
};

const Page = () => {
  return <Success />;
};

export default Page;
